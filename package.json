{
  "name": "keepalive-new",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "format": "biome format --write ./src",
    "lint": "biome lint ./src",
    "check": "biome check --apply ./src",
    "build": "bun run ./build.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.6.3",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
